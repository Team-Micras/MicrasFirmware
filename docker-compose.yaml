services:
  base:
    build:
      target: micras

  build:
    command: /MicrasFirmware/docker/scripts/build.sh
    build:
      target: build

  lint:
    command: /MicrasFirmware/docker/scripts/build.sh -DLINTER_MODE=ON
    build:
      target: build

  format:
    command: /MicrasFirmware/docker/scripts/format.sh
    build:
      target: build

  dev:
    command: bash
    build:
      target: micras
    privileged: True
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
      - NVIDIA_DRIVER_CAPABILITIES=all
    volumes:
      - .:/MicrasFirmware:rw
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ${XAUTHORITY:-$HOME/.Xauthority}:/root/.Xauthority
